module GlobalPollenProject.App.Tests.Projections

open GlobalPollenProject.App.Projections
open Xunit

module ``When a grain's identity is confirmed`` =

    [<Fact>]
    let ``It is appended to it's direct match in the MRC`` () =
        
        2.
    
    [<Fact>]
    let ``An MRC taxon is created if it doesn't exist`` () = 2.
    
    [<Fact>]
    let ``It is appended to any upstream family and/or genus`` () = 2.
    
    [<Fact>]
    let ``Upstream taxa are added to the MRC if they don't exist`` () = 2.
    
    
module ``When a reference collection is published`` =
    
    [<Fact>]
    let ``Some test...`` () = 2.